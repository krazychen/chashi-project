whyy-system:
  # 是否启用ansi控制台输出有颜色的字体，local环境建议开启，服务器环境设置为false
  enable-ansi: false

server:
  port: 1111
  servlet:
    context-path: /
spring:
  datasource:
    url: jdbc:mysql://192.168.0.103:3306/whyy-system?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Springboot666!

  # Redis配置
  redis:
    database: 0
    host: localhost
    password:
    port: 6379

